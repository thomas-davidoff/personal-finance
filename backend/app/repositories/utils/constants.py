from app.models import Category, Account, Transaction, Budget, Keyword

MODEL_NAMES = {
    'category': Category,
    'keyword': Keyword,
    'account': Account,
    'transaction': Transaction,
    'budget': Budget
}